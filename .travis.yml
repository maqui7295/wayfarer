before_script:
  - yarn install
  - psql -c 'create database travis_ci_test;' -U postgres
  - yarn run initdb
language: node_js
node_js:
  - 'stable'
  - '12.14.0'
cache:
  directories:
    - 'node_modules'

services:
  - postgresql
